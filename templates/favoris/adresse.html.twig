{% extends 'base.html.twig' %}

{% block title %}Hello addresse!{% endblock %}

{% block body %}
<div class="container mb-80 mt-50">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="row">

                            <h4 class="mb-30">Vos informations</h4>
                            <div class="billing-detail-blk">
                            {{ form_start(form) }}
                                    <div class="row">
                                        <div class="form-group col-lg-12">
                                        {{ form_row(form.email) }}
                                        </div>
                                    </div>
                                    <div class="row">
                                        <div class="form-group col-lg-12">
                                            {{ form_row(form.numero) }}
                                        </div>
                                    </div>
                                    <div class="row">
                                        <div class="form-group col-lg-12">
                                            <button  type="submit" class="btn btn-primary">Envoyer</button>
                                        </div>
                                    </div>
                            {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
{% endblock %}
