{% extends 'base.html.twig' %}

{% block title %}SlideHome{% endblock %}

{% block body %}
    <h1>SlideHome</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slide_home.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ slide_home.image }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slide_home.Titre }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ slide_home.contenue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_slide_home_index') }}">back to list</a>

    <a href="{{ path('app_slide_home_edit', {'id': slide_home.id}) }}">edit</a>

    {{ include('slide_home/_delete_form.html.twig') }}
{% endblock %}
